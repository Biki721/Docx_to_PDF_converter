// Configure the API base URL here if hosting frontend separately from backend.
// Examples:
// window.API_BASE = 'http://127.0.0.1:8000'; // FastAPI default
// window.API_BASE = 'http://127.0.0.1:3001'; // Express default
// Leave empty to use same-origin (recommended when serving frontend from the backend):
window.API_BASE = "http://localhost:8000";
